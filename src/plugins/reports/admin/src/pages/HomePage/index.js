/*
 *
 * HomePage
 *
 */

import React, { memo } from 'react';
// import PropTypes from 'prop-types';
import pluginId from '../../pluginId';

const HomePage = () => {
  return (
    <div style={{display: 'flex'}}>
      <h1 style={{fontSize: "1.4rem"}}>Gráficos</h1>
    </div>
  );
};

export default memo(HomePage);
